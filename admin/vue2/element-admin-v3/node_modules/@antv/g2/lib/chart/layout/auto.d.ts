import { Padding } from '../../interface';
import View from '../view';
/**
 * @ignore
 * 根据 view 中的组件，计算实际的 padding 数值
 * @param view
 */
export declare function calculatePadding(view: View): Padding;
