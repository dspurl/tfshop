import { G2PlotTheme, G2Theme, Style } from './interface';
/**
 * 将 主题 转换为 G2 主题配置
 * @param type plotType
 */
export declare function convertThemeToG2Theme(type: string /** plot style */, theme: G2PlotTheme | Style): G2Theme;
/**
 * 注册新的图表主题
 * @param type
 * @param theme
 */
export declare function registerTheme(type: string, theme: G2PlotTheme | Style): void;
/**
 * 根据类型获取主题
 * @param type plotType, such as line, column, bar, pie, bullet, radar and so on
 */
export declare function getTheme(type: string): G2Theme;
