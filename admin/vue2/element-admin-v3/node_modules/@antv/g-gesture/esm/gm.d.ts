import EE from '@antv/event-emitter';
import { IElement } from '@antv/g-base';
import { Options } from './interface';
/**
 * 给 view 添加移动端的滚动能力：
 *  - 监听 touch 事件，emit wheel 事件
 *  - 滚动惯性包装
 *  - 和 pc 的 wheel 事件保持基本一致
 */
export declare class GM extends EE {
    private element;
    private options;
    private gestures;
    constructor(element: IElement, options?: Options);
    /**
     * 销毁，取消事件绑定
     */
    destroy(): void;
    /**
     * 实例化手势
     */
    private initialGestures;
    /**
     * 绑定 touchstart 事件
     */
    private bindTouchStart;
    private preventEvent;
    /**
     * touchstart 触发
     * @param ev
     */
    private onTouchStart;
    /**
     * touchmove 触发
     * @param ev
     */
    private onTouchMove;
    /**
     * touchend 触发
     * @param ev
     */
    private onTouchEnd;
    /**
     * touchcancel 触发
     * @param ev
     */
    private onTouchCancel;
    private doGestures;
}
