import EE from '@antv/event-emitter';
import { IElement } from '@antv/g-base';
import { Event } from './event';
/**
 * 给 view 添加移动端的滚动能力：
 *  - 监听 touch 事件，emit wheel 事件
 *  - 滚动惯性包装
 *  - 和 pc 的 wheel 事件保持基本一致
 */
export declare class Wheel extends EE implements Event {
    private element;
    private gm;
    private raf;
    private rafMs;
    private ms;
    constructor(element: IElement);
    destroy(): void;
    /**
     * pan 事件
     * @param ev
     */
    private onPan;
    /**
     * 当出现 swipe 事件的时候
     * @param ev
     */
    private onSwipe;
    private rafInertia;
    private getWrapperEvent;
}
